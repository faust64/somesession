{
  "name": "somesession",
  "license": "BSD-3-Clause",
  "version": "0.0.1",
  "dependencies": {
    "wraplog": "^0.0.2"
  },
  "devDependencies": {
    "aerospike-session-store": "latest",
    "axios": "latest",
    "bluebird": "latest",
    "body-parser": "latest",
    "cassandra-session-store": "latest",
    "connect-memcached": "latest",
    "connect-mongodb-session": "latest",
    "connect-pg-simple": "latest",
    "connect-redis": "latest",
    "connect-sqlite3": "latest",
    "express": "latest",
    "express-mysql-session": "latest",
    "express-session": "latest",
    "mocha": "5.0.2"
  },
  "description": "Express.js sessions store abstraction library",
  "engines": {
    "node": "6.9.1"
  },
  "repository": "https://github.com/faust64/somesession",
  "scripts": {
    "release": "(npm whoami | grep '^Not authed' >/dev/null && npm login) ; npm whoami | grep '^Not authed' >/dev/null && echo 'login to npm required' >&2 && exit 1 ; git push -u origin master && npm publish",
    "test": "make test"
  }
}
